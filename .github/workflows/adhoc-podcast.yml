name: Ad Hoc Podcast (Paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Ad-hoc publication is paused until it uses the same evidence manifest and transactional publisher as the daily edition."
